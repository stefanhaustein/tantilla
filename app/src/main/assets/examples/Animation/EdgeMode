def main():
 let ball = screen.newSprite()
 ball.face = "🎱"
 ball.height = 30
 ball.edgeMode = EdgeMode.BOUNCE
 ball.rotation = 300
 ball.dx = -57
 ball.dy = 31
 ball.say "EdgeMode.BOUNCE"
 let ghost = screen.newSprite()
 ghost.face = "👻"
 ghost.height = 30
 ghost.edgeMode = EdgeMode.WRAP
 ghost.opacity = 0.7
 ghost.dx = 13
 ghost.dy = 7
 ghost.say "EdgeMode.WRAP"
end